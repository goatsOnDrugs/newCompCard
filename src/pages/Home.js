import React from 'react';

const Home = props => {
  return (
    <div
      style={{
        height: 300,
        width: 300,
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
      }}
    >
      Home Page
    </div>
  );
};

export default Home;
